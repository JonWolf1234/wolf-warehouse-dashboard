window.WAREHOUSE_CONFIG = {
  // During local testing this can remain blank because the same Node app serves the page and API.
  // After deploying Render, replace this with your Render URL, without a trailing slash.
  // Example: "https://wolf-warehouse-dashboard-api.onrender.com"
  apiBaseUrl: "",

  refreshSeconds: 60,
  defaultDaysAhead: 30,
  companyName: "WOLF",
  screenTitle: "Warehouse Schedule"
};
