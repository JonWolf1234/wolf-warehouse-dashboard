{
  "name": "wolf-warehouse-dashboard",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Warehouse job preparation dashboard for Current RMS",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "check": "node --check server.js && node --check src/current-rms.js && node --check src/normalise.js && node --check docs/app.js"
  },
  "dependencies": {
    "compression": "^1.8.1",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.3"
  }
}
