# Current RMS Member Inspection

Adds a read-only `Inspect Current RMS member` button inside Allocation details.

The diagnostic probes:
- `/members/:id`
- nested member stock levels
- nested member resources
- nested member bookable resources
- filtered stock levels
- filtered resources

No Current RMS data is changed.
