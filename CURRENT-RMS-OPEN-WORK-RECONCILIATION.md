# Open Work reconciliation

- Only confirmed Current RMS orders create freelancer vacancies.
- Quotes and provisional opportunities remain visible on normal dashboards.
- Filled vacancies are automatically closed.
- If an allocation is later removed and the vacancy returns, an automatically
  closed publication returns to Draft / Awaiting review.
- Manually closed positions remain closed.

Run `npm run migrate`, restart, then manually sync Current RMS.
